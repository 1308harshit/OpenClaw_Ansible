all:
  children:
    fabric:
      children:
        leafs:
          hosts:
            leaf1:
              ansible_host: leaf1
            leaf2:
              ansible_host: leaf2
            leaf3:
              ansible_host: leaf3
            leaf4:
              ansible_host: leaf4
        spines:
          hosts:
            spine1:
              ansible_host: spine1
            spine2:
              ansible_host: spine2
        routers:
          hosts:
            R1:
              ansible_host: R1

    localhost_linux:
      vars:
        ansible_connection: local
        ansible_become_method: sudo
        ansible_become_password: ansible
      hosts:
        localhost:
